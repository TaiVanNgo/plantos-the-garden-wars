#include "../../include/plants.h"

void draw_peashooter(int x, int y)
{
  draw_image(peashooter, x, y, PLANT_WIDTH, PLANT_HEIGHT, 0);
}
void draw_sunflower(int x, int y)
{
  draw_image(sunflower, x, y, PLANT_WIDTH, PLANT_HEIGHT, 0);
}
void draw_sunflower_unhappy(int x, int y)
{
  draw_image(sunflower_unhappy, x, y, PLANT_WIDTH, PLANT_HEIGHT, 0);
}
void draw_frozen_peashooter(int x, int y)
{
  draw_image(frozen_peashooter, x, y, PLANT_WIDTH, PLANT_HEIGHT, 0);
}
void draw_chillies(int x, int y)
{
  draw_image(chillies, x, y, PLANT_WIDTH, PLANT_HEIGHT, 0);
}
void draw_chillies_unhappy(int x, int y)
{
  draw_image(chillies_unhappy, x, y, PLANT_WIDTH, PLANT_HEIGHT, 0);
}
void draw_wallnut(int x, int y)
{
  draw_image(wallnut, x, y, PLANT_WIDTH, PLANT_HEIGHT, 0);
}
void draw_wallnut_unhappy(int x, int y)
{
  draw_image(wallnut_unhappy, x, y, PLANT_WIDTH, PLANT_HEIGHT, 0);
}
void draw_bullet_blue(int x, int y)
{
  draw_image(bullet_blue, x, y, BULLET_WIDTH, BULLET_HEIGHT, 0);
}
void draw_bullet_green(int x, int y)
{
  draw_image(bullet_green, x, y, BULLET_WIDTH, BULLET_HEIGHT, 0);
}