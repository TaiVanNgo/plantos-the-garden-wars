# EEET2490-assignment-3

```md
📁 The Garden Wars/
├── .vscode/         # VS Code configuration files (build tasks, debug setup)
├── assets/          # Game assets such as images, fonts, and sprite data
├── build/           # Compiled output files (e.g., .o, .elf binaries)
├── include/         # Header files (.h) with function declarations and shared constants
├── src/             # Source code (.c) implementing the core game and OS logic
├── .gitignore       # Specifies files and folders to be ignored by Git
├── Makefile         # Defines build rules and compilation instructions
├── note.md          # Developer notes or in-progress documentation
└── README.md        # Main documentation file with project overview and usage
```
